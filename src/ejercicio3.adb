with Ada.Integer_Text_IO;
use Ada.Integer_Text_IO;
with Ada.Float_Text_IO;
use ada.Float_Text_IO;
with pkg_ejercicio2;
use pkg_ejercicio2;

procedure ejercicio3 is
   notamedia : Standard.Float := getNotaMedia();
begin
   put(numAlumnos);
   put(notamedia);


end ejercicio3;
